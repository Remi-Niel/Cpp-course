There are two v_pointers, as there are two objects of classes with virtual member functions (the Message objects only contain a pointer to the existing Base objects). There is one vtable shared between the objects of class Base.